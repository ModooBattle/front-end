import { styled } from 'styled-components';

const Title = styled.h3`
	padding-top: 44px;
	font-size: var(--fs-5);
	font-style: normal;
	font-weight: 700;
	line-height: 150%; /* 36px */
	letter-spacing: -1px;
`;

export default Title;
